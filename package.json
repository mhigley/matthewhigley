{
  "name": "gatsby-starter-default",
  "private": true,
  "description": "A simple starter to get up and developing quickly with Gatsby",
  "version": "0.1.8",
  "author": "Kyle Mathews <mathews.kyle@gmail.com>",
  "dependencies": {
    "gatsby": "^2.3.2",
    "gatsby-image": "^2.0.35",
    "gatsby-plugin-manifest": "^2.0.24",
    "gatsby-plugin-offline": "^2.0.25",
    "gatsby-plugin-prefetch-google-fonts": "^1.4.0",
    "gatsby-plugin-react-helmet": "^3.0.11",
    "gatsby-plugin-sass": "^2.0.11",
    "gatsby-plugin-sharp": "^2.0.31",
    "gatsby-source-filesystem": "^2.0.28",
    "gatsby-transformer-sharp": "^2.1.17",
    "node-sass": "^4.11.0",
    "prop-types": "^15.7.2",
    "react": "^16.8.5",
    "react-dom": "^16.8.5",
    "react-helmet": "^5.2.0"
  },
  "devDependencies": {
    "@fortawesome/fontawesome-pro": "^5.8.1",
    "prettier": "^1.16.4"
  },
  "keywords": [
    "gatsby"
  ],
  "license": "MIT",
  "scripts": {
    "build": "gatsby build",
    "develop": "gatsby develop",
    "format": "prettier --write src/**/*.{js,jsx}",
    "start": "npm run develop",
    "serve": "gatsby serve",
    "test": "echo \"Write tests! -> https://gatsby.dev/unit-testing\"",
    "function": "node packageScript.js",
    "version:major": "npm version major",
    "version:minor": "npm version minor",
    "version:patch": "npm version patch",
    "gatsby:build": "gatsby build",
    "git:add": "git add .",
    "git:commit:major": "git commit -m \"major $npm_package_version update\"",
    "git:commit:minor": "git commit -m \"minor $npm_package_version update\"",
    "git:commit:patch": "git commit -m \"patch $npm_package_version update\"",
    "git:push": "git push",
    "netlify:major": "npm run gatsby:build && npm run git:add && npm run git:commit:major && npm run version:patch && npm run git:push",
    "netlify:minor": "npm run gatsby:build && npm run git:add && npm run git:commit:minor && npm run version:patch && npm run git:push",
    "netlify:patch": "npm run gatsby:build && npm run git:add && npm run git:commit:patch && npm run version:patch && npm run git:push"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/gatsbyjs/gatsby-starter-default"
  },
  "bugs": {
    "url": "https://github.com/gatsbyjs/gatsby/issues"
  }
}
